#include <stdio.h>
int main() {
    int n, sum=0, temp; scanf("%d",&n);
    temp=n;
    while(n>0){ int r=n%10; sum+=r*r*r; n/=10; }
    printf("%d is %s\n", temp, (sum==temp)?"Armstrong":"Not Armstrong");
    return 0;
}
