#include <stdio.h>
int main() {
    char c;
    printf("Enter a character: ");
    scanf(" %c", &c);
    if((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
        printf("Alphabet\n");
    else
        printf("Not an Alphabet\n");
    return 0;
}
